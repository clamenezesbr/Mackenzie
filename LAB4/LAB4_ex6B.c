#include <stdio.h>

int funcao(int N) {
  return 0; // substitua esta linha pela função que você implementou
}

int main() {
  int N;
  printf("Digite um valor numérico:\n");
  scanf("%d", &N);
  printf("O valor retornado pela função é %d\n", funcao(N));
return 0;
}